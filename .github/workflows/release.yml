name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Compute version
        id: version
        run: echo "value=v$(date +%Y.%m.%d)-${{ github.run_number }}" >> "$GITHUB_OUTPUT"

      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        env:
          IMAGE: ghcr.io/the127/keyline-docs
          VERSION: ${{ steps.version.outputs.value }}
        run: |
          docker build -f Containerfile -t "$IMAGE:$VERSION" -t "$IMAGE:latest" .
          docker push "$IMAGE:$VERSION"
          docker push "$IMAGE:latest"

      - uses: sigstore/cosign-installer@v3

      - uses: aquasecurity/trivy-action@master
        with:
          scan-type: image
          image-ref: ghcr.io/the127/keyline-docs:${{ steps.version.outputs.value }}
          format: cosign-vuln
          output: vuln.json

      - name: Attest vulnerabilities
        env:
          IMAGE: ghcr.io/the127/keyline-docs
          VERSION: ${{ steps.version.outputs.value }}
        run: |
          cosign attest --predicate vuln.json --type vuln --yes "$IMAGE:$VERSION"

      - name: Create release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.version.outputs.value }}
          name: ${{ steps.version.outputs.value }}
          generate_release_notes: true
